#include<stdio.h>
int main(){
   /* int n=1,times=6;
    while (n<=times){
        printf(" n=%d\n",n);
        n++;
    }
    */
  // for (int x=5;x>=1;x=x-1){
   // printf("the value of x :%d\n",x);
   //}
   int x;
   for(x=6;x>=1;x-1) {
    for(int y=1;y<=x;y+1)
    printf("the value of y is %d \n and the value of x is %d\n",x,y);

   }
    break;
    return 0;
}
