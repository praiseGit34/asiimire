#include <stdio.h>
#include <math.h>
float mean(int marks[] , int n){
    float sum;
    float i;
    for(int i=0;i<n;i++);
    sum=sum + marks[i];
    printf("%d",marks[i]);
}
float sd(grades[] , int size);{
    float squaresum=0;
    for(int 1=0;i<size;i++);
    float square= grade[i]*grade[i];
    squaresum+=square;
    float sum=0;
    float average=mean(grade,size);
    float standarddeviation=sqrt((squaresum/size) -(average*average));
    printf("the standard deviation is %4f and the mean is %3f",standard deviation,mean);
    return standarddeviation;
}
int main(){
    int size=2;
    
    
  
    return 0;
}